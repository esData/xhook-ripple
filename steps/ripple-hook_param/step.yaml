---
name: ripple-hook_param
version: 0.1.0
summary: Hook param implementation
description: A step introduce hook parameters
tags:
  - xrpl_hook_param
  - src
templates:
  ripple-trace:
    main: ripple-main.c.mustache
    body:
      - ripple-hook_param.c.mustache
parameters:
  param_name:
    description: Hook param name, to be applied under Step Hook or Hook WASM payload deploy
    type: string
  param_field:
    description: Source level parameter name, ensure the parameter name was consistant across source.
    type: string
