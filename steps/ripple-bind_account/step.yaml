name: ripple-bind_account
version: 0.1.0
summary: continue only transactions was prepare/from a account.
description: Bind account in WASM implementation
tags:
  - accept
templates:
  main: main.c.mustache
  body:
    - bind_account.c.mustache
parameters:
  reason:
    description: Reason or references code
    type: string
    default: SUCCESS

