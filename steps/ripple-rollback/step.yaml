---
name: ripple-rollback
type: step
version: 0.1.0
summary: rollback/reject a transaction and prepare the WASM
description: Rollback or revert a transaction. note transaction charges still deducted from account.
tags:
  - reject
  - wasm
  - final_src
templates:
  ripple-accept:
    main: ripple-main.c.mustache
    body:
      - ripple-reject.c.mustache
parameters:
  reason:
    description: Reason or references code
    type: string
    default: REJECTED
